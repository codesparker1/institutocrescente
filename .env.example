# Neon (Postgres)
DATABASE_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require"
DIRECT_URL="postgresql://user:password@host.region.aws.neon.tech/dbname?sslmode=require"

# Auth.js — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""

# Vercel Blob — documentos do aluno (src/actions/documentos.ts). Criar um Blob Store no dashboard
# da Vercel, ligar ao projeto, e copiar o token gerado (ou `vercel env pull` se já ligado).
BLOB_READ_WRITE_TOKEN=""
